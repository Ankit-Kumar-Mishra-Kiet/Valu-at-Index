/User function template for C++
class Solution{
public:

	vector<int> valueEqualToIndex(int arr[], int n) {
	    int i;
	    vector<int>res;
	    for(i=0;i<n;i++){
	        if(arr[i]==i+1)
	        res.push_back(arr[i]);
	        }
return res;
	}
		
};
